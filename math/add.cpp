#include <bits/stdc++.h>
using namespace std;
using uint = unsigned int;
using ll = long long;
using vi = vector<int>;
using vll = vector<ll>;

#define ip for(int i=0; i<n; ++i) cin>> v[i]
#define f(i, start, end) for(int i=start; i<end; ++i)

#define pb push_back
#define all(v) v.begin(), v.end()
#define rall(v) v.rbegin(), v.rend()

void solve() {
    int a, b;
    cin>> a>> b;
    cout<< a-b<< endl;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin>> t;
    while(t--) {
        solve();
    }
    return 0;
}